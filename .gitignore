# Deepwork session state (keep local, not in git)
.slim/deepwork/

# Dependencies
node_modules/

# mise-msb transient build outputs
.mise-msb-build/

# mise-msb transient setup outputs
.mise-msb-setup/
