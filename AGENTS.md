# Project instructions

- Use Bun for this project's JavaScript/TypeScript workflow.
- Prefer `bun install`, `bun test`, `bun run <script>`, and `bunx <tool>` over npm, pnpm, or yarn.
- When adding or updating repo instructions, keep Bun as the default runtime and package manager unless a task explicitly requires another tool.
